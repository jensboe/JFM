# -- FILE: pytest.ini (or tox.ini)
[pytest]
DJANGO_SETTINGS_MODULE = jfm.settings
# -- recommended but optional:
python_files = tests.py test_*.py *_tests.py
addopts = --cov-branch --cov=. --cov-report html --cov-report term --cov-report lcov